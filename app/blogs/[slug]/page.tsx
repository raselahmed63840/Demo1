import Link from "next/link";
import { blogs } from "@/lib/data";
import { PageHero, Storefront } from "@/components/storefront";
import { notFound } from "next/navigation";
export default async function BlogDetails({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const blog = blogs.find((item) => item.slug === slug); if (!blog) notFound(); return <Storefront><PageHero eyebrow={blog.category} title={blog.title} text={blog.date} /><article className="article container"><img src={blog.image} alt={blog.title} /><p className="article-lead">{blog.excerpt}</p><p>Shopping works best when it starts with a clear need. Consider where you will use the product, which features matter every day, and what makes it easy to maintain over time.</p><h2>Focus on practical value</h2><p>Compare the essentials first: quality, compatibility, delivery options and seller support. A thoughtful shortlist makes every purchase feel more confident.</p><h2>Make the choice yours</h2><p>Trends can inspire you, but the best purchase is the one that suits your routine. Explore the collection, read product details and choose what fits your day.</p><Link href="/blogs" className="button button-outline">← Back to journal</Link></article></Storefront>; }
